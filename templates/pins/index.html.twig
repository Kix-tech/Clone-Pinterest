{% extends 'base.html.twig' %}

{% block title %}Pinterest{% endblock %}

{% block body %}
<h1>{{pluralize(pins|length,'Pin')}}</h1>

    {%for pin in pins%}
        <h2>{{pin.title}}</h2>
        <p>{{pin.description}}</p>
    {%endfor%}

{% endblock %}
